before_script:
  - apt-get -qq update && apt-get -y -qq install gnu-efi mtools gdisk gcc make g++ dosfstools

make:
  script:
    - cd src/kernel/arch/amd64-uefi
    - make compressed-image
  artifacts:
    paths:
      - src/kernel/arch/amd64-uefi/hd.img.gz
      - src/kernel/arch/amd64-uefi/main.efi
  
