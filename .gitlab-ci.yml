image: registry.lf-net.org/littlefox/lf-os_amd64/ci:2019082701
stages:
- test
- build

test:
  stage: test
  script:
    - make test

build:
  stage: build
  script:
    - make hd.img.gz lf-os.iso.gz
  artifacts:
    paths:
      - hd.img.gz
      - lf-os.iso.gz
