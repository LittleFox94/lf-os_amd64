make:
  image: debian:buster
  before_script:
    - apt-get -qq update && apt-get -y -qq install gnu-efi mtools gdisk gcc make g++ dosfstools nasm
  script:
    - make hd.img.gz
  artifacts:
    paths:
      - hd.img.gz
