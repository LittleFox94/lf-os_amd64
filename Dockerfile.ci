FROM debian:bullseye

RUN apt-get -qq update && \
    apt-get -y -qq install gnu-efi mtools gdisk clang lld make dosfstools gcc genisoimage doxygen libyaml-perl rsync cmake ccache g++ python3-distutils && \
    rm -rf /var/cache/apt
